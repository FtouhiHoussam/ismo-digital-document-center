# Base URL for the backend API.
# During development this is usually left empty because Vite proxies
# /api/* to http://localhost:5000 automatically (see vite.config.js).
#
# In production (or if you disable the proxy) set this to your backend URL:
# VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=
